using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class RainRotation : MonoBehaviour {
    /*
	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
        if (GameController.Game.Orientation.sqrMagnitude > .1) {
            Vector3 rot = new Vector3(0, 0, (Mathf.Atan2(GameController.Game.Orientation.y, GameController.Game.Orientation.x) * 180f / Mathf.PI) + 90);
            //if (rot.z >= -100 && rot.z <= 100) {
                transform.rotation = Quaternion.Euler(rot);
            //}
        }
    }
    */
}
